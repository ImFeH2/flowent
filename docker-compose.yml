services:
  autopoe:
    image: ${AUTOPOE_IMAGE:-imfeh2/autopoe:latest}
    working_dir: /workspace
    ports:
      - "${AUTOPOE_PORT:-6873}:6873"
    volumes:
      - ./data:/root/.autopoe
      - ./workspace:/workspace
    restart: unless-stopped
    init: true
