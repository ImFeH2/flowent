IDLE_USAGE_GUIDANCE = """\
## Idle Usage Rules

- `idle` means you are temporarily waiting for a new message to continue.
- Only use `idle` when you have finished the current step of work, or you are blocked waiting for another agent or the Human.
- Do not use `idle` if you still have an immediate action you can take now.
- Do not use `idle` instead of replying to a newly received message.
- After calling `idle`, you will be re-activated when a new message arrives.
"""
