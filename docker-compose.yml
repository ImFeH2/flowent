services:
  autopoe:
    image: ${AUTOPOE_IMAGE:-imfeh2/autopoe:latest}
    ports:
      - "${AUTOPOE_PORT:-8000}:8000"
    volumes:
      - autopoe-data:/data
    restart: unless-stopped
    init: true

volumes:
  autopoe-data:
